# Project Backlog

Running log of infrastructure, feature, and bug work. Items are logged here for
tracking only — nothing on this list is being executed until picked up
explicitly in a future session.

Each item is tagged with its source: `[user]` or `[claude]` (raised during dev).

Status values: `open` (default, omitted below), `in-progress`, `done`.

## Infrastructure

- [x] Verify git/GitHub coverage: confirm the entire project (all files and data) is checked in to local git and pushed to GitHub, nothing important is only local or gitignored `[user]` — done 2026-08-12: working tree clean, both local branches match `origin` exactly, no stashes, `.gitignore` only excludes caches/venvs/generated `output/*` (sample run stays committed). Two findings surfaced, not fixed here: (1) repo has no `main`/`master` — GitHub's default branch is literally `claude/travel-activity-book-generator-09n1i0`, and PR #1 targets that branch; (2) the repo is **public**, which sharpens the existing "release without exposing files/data/symbols" item above.
- [ ] Check user web form interaction (audit the actual UX flow end-to-end) `[user]`
- [ ] Decide how to release without exposing all project files, data, and internal symbols `[user]`
- [x] Figure out how many image generations are actually needed (scope/cost for the `ImageBackend` seam) `[user]` — done 2026-08-13: already computed and surfaced per run via each page's `needs_illustration` metadata flag (`False` only for `word_search`/`crossword`, since those are fully typeset puzzles) — `src/web.py`'s result page already reports it: "N pages want a picture... the other M are already finished."
- [x] Delete all unused prompt files `[user]` — done 2026-08-13: `output_writer.py`'s `_warn_about_stale()` already detects leftover `prompts/*.md` files from an earlier, longer run and logs a warning rather than silently deleting them (deliberate, so nothing a user might still want is auto-removed). Nothing to delete right now — the committed sample has exactly 14 prompt files for its 14 pages.
- [ ] Rename `output/kfar-hanokdim/` (the committed sample run) to something like `output/example/`, and reconsider whether it's still relevant to keep — revisit later `[user]`
- [ ] Add CI (e.g. GitHub Actions) to run `pytest` automatically on push/PR — no workflow currently exists `[claude]`
- [x] Add a dependency manifest (`pyproject.toml` / `requirements.txt`) pinning versions, including Playwright for `--pdf` — none exists today; overlaps with the release task above `[claude]` — done 2026-08-13: added `pyproject.toml` (`[project]` metadata, `requires-python >= 3.11`, empty core `dependencies` since the generator stays stdlib-only) with `pdf` (`playwright>=1.40,<2`) and `dev` (`pytest>=7,<10`) optional-dependency groups; README's Playwright install line and CLAUDE.md's tooling note updated to match. No lockfile/exact pins — this is a library-style manifest with minimum-version ranges, not a deployable app needing a frozen lockfile.
- [ ] Add test coverage tooling/reporting (e.g. `pytest-cov`) — no coverage measurement configured, separate from the git/GitHub coverage check above `[claude]`
- [ ] Add rotation/retention policy for the web form's analytics sink (`output_root/.analytics/events.jsonl`), which currently grows unbounded `[claude]`
- [ ] Set `main` as the repo's actual default branch on GitHub (Settings → Branches) — the `main` branch and history now exist and PR #1 targets it, but no available tool can flip the repository setting itself; needs a human click `[claude]`
- [x] Set up a `main` branch as the project trunk `[user]` — done 2026-08-12: created `main` from the tip of `claude/travel-activity-book-generator-09n1i0` (the prior default) and pushed it; retargeted PR #1's base onto `main`. Flipping GitHub's actual "default branch" setting still needs a human (see item above); old branch left in place, not deleted.
- [x] Keep `.env`, output files, and user-uploaded files out of GitHub / non-local exposure `[user]` — done 2026-08-12: `.env` was never gitignored (no `.env` in use today, added defensively); the `output/kfar-hanokdim/` exception un-ignored the whole directory tree, so a real reference-photo upload (`src/web.py`'s `_save_photos`, saved under `output/<slug>/reference/`) or regeneration for that destination would have ridden along un-ignored — tightened to an explicit allowlist of just the 5 committed sample files, verified with `git check-ignore`. Confirmed via `git ls-files output/` that no other destination's output is tracked, staged, or untracked-but-present anywhere under `output/`.

## Feature

- [ ] Compare generated output with user edits; analyze the diffs to see which changes can be solved upstream in generation instead `[user]`
- [ ] Understand how local/destination data is created; explore tuning it to be more kid-friendly and less overly specific `[user]`
- [ ] Explore an automatic flow from doodle-style reference images to sorted local destination data `[user]`
- [ ] Validate and improve the quality of generated image prompts `[user]`
- [ ] Maze activity feels boring — explore ways to enrich the experience `[user]`
- [ ] Add a workbook-completion flow: a generated workbook isn't "done" until every page's art exists — track and show completion as a percentage / remaining-steps count, walk the user through each page's image prompt (`prompts/NN_<activity>.md` / `ImageBrief`) letting them choose per page between auto-generating the image (once `ImageBackend` is implemented) or uploading their own art, and mark the workbook complete once every page has art `[user]`
- [ ] Visual QA pass on the Hebrew/RTL layout (`HtmlRenderer` / PDF output) — only one non-English locale exists and RTL-specific rendering bugs are easy to miss without actually looking at output `[claude]`
- [ ] Manually verify the map activity's double-page centre spread, print-checked as an actual A4-landscape page (not just a headless-Chromium measurement): stop labels legible and clear of the fold, no stray marks/placeholder artifacts, path/labels read correctly at real print size in both LTR and RTL books `[user]` — raised after a Hebrew Pelion book showed English stop names ("Volos, Makrinitsa, Milies, Tsagarada") on the map's subtitle; that specific leak is expected and not itself a bug (a free-text itinerary has no translation path — see the Bug item below), but the map page's actual print appearance hasn't had a human look since the A5 centre-spread work landed.

## Bug

- [x] Word search words must not be editable/changeable after generation (data integrity between word list and grid) `[user]` — done 2026-08-13: `word_search.py`'s `_build_grid` generates the grid and word list together in one step, both stored in the same page metadata that the print layout typesets directly (`layouts.py:_word_search`). No CLI, web form, or renderer path lets words be edited independently of the grid, so there's no live mechanism for the two to drift apart.
- [x] `src/uploads.py`'s hand-rolled multipart parser has no max body/file-size limit — a large upload to the web form could exhaust memory `[claude]` — done 2026-08-13: `web.py` already capped total request body at 32 MB before reading it off the socket, but a single file within that budget was unbounded. Added `MAX_FILE_BYTES` (10 MB) in `uploads.py` itself so the parser enforces a per-file cap independently of the caller, raising `UploadError` (already surfaced as a 400 by `web.py`). Covered by a new test in `tests/test_web.py`; full suite passes.
- [x] Web form result page showed a "Some English may have slipped in" notice to the parent, even when the leak is expected and not actionable by them (e.g. a free-text itinerary typed in English inside a Hebrew book — `src/qa.py` has no way to translate arbitrary user text) `[user]` — done 2026-08-16: `_render_result` (`src/web.py`) no longer surfaces `language_qa` findings on the result page. The check still runs on every generation (`find_english_leaks`, gated by `tests/test_localization.py`) and now feeds `Report.qa_leaks` (`src/analytics.py`) via the `book_created` event's `language_qa_leaks` count, visible on `/stats` under "Possible English leaks" for actual triage instead. Covered by two new tests in `tests/test_web.py`.
- [x] Drawing activity's blank frame showed placeholder copy ("Your drawing goes here" / "Generate this picture from the prompt file, then drop it in.") explaining how to create artwork the page never has a prompt for — reads as instructions in the middle of what is supposed to be an empty page for the child to draw on `[user]` — done 2026-08-16: `_drawing` (`src/rendering/layouts.py`) now renders a genuinely empty `.art` frame (new `art_blank.html.tmpl` / `LayoutContext.blank_art()`), no label or note. Removed the now-unused `pdf.draw_here` locale key from both `en.py` and `he.py`.
- [x] Matching ("shadow") page's gutter carried a "draw your lines across here" note, contradicting the page's own design intent (its docstring already said "no words inside the working area at all: a pre-reader can do it") `[user]` — done 2026-08-16: dropped `gutter_note` from `_matching` (`src/rendering/layouts.py`) and `body_matching.html.tmpl`; removed the now-unused `pdf.match_gutter` locale key from both `en.py` and `he.py`. The gutter itself (the empty space a child draws a connecting line across) is unchanged.
- [x] Matching page could offer a symbol that doesn't fit the destination's climate/setting (e.g. a woolly winter hat for warm-climate Pelion) `[user]` — done 2026-08-16: `MatchingActivity._choose` (`src/activities/matching.py`) now prefers universal-pool symbols whose `environments`/`climate` facets fit the destination's profile (`src.symbols.profile.profile_for`), the same veto rule `packing.py` already applies to its own pool, falling back to the unfiltered bank only if too few symbols fit (the page must never come up short of pairs). Pelion never draws `woolly-hat`/`gloves` across 30 seeds tried. 4 new tests in `tests/test_activities.py`.
- [x] Scavenger hunt sheets could end up dominated by animals by chance (the universal pool's animal topic is its deepest, 16 of 45 entries, and destination wildlife leans the same way) `[user]` — done 2026-08-16: `ScavengerHuntActivity._cap_animals` (`src/activities/scavenger_hunt.py`) caps a sheet at 3 animal-topic symbols, trimming universal-pool animals before ever touching a local/destination-sourced one (a local wildlife sight is the scarce, trip-specific ingredient the page is partly built around — see the module docstring) and falling back to keeping an extra animal rather than shrinking the sheet if the non-animal pools ever came up short. Verified across 270 (destination × seed × difficulty) runs, max 3 every time. 2 new tests in `tests/test_scavenger_hunt.py`, including the exact regression this needed fixing once already (a two-sight, both-animals wildlife pool losing both sights to a larger crop of universal animals).
- [x] Drawing page's instructions still named a specific subject pulled from the destination's knowledge ("Draw what you remember about {subject} inside the frame..."), and could land right before the closing reflection page — which has its own blank drawing box — reading as the same open-ended drawing page twice in a row `[user]` — done 2026-08-16: `DrawingActivity.generate` (`src/activities/drawing.py`) no longer samples a subject; `drawing.instructions` (`en.py`/`he.py`) is now one constant, generic line ("Draw a detailed scene from your memory...") in every book, and the now-unused `drawing.instructions_prompted` key was removed. `WorkbookPlanner._space_out_drawing_from_reflection` (`src/agents/planner.py`) keeps "drawing" at least 3 body slots clear of the tail whenever "reflection" closes the book — verified this puts it on page 8 of a 12-page book (the position the user suggested) and holds across destinations, seeds and page counts. New tests in `tests/test_planner.py` and `tests/test_activities.py`.
- [x] After editing a workbook in the browser, "Save as PDF" (`book.html.tmpl`'s in-browser editor) could only save at A4, one page per sheet — never the book's real A5-booklet-two-up-on-A4 format — because it called the browser's own `window.print()`, which only ever offers loose page sizes and has no notion of this project's fold-and-staple imposition `[user]` — done 2026-08-16: the button now POSTs the edited document to a new `/print` endpoint (`src/web.py`) that runs it through the same headless-Chromium + `impose_booklet` pipeline `--pdf` uses (`PdfRenderer.render_html`, new — prints already-rendered markup with no `Workbook` involved), returning the correctly imposed `workbook-booklet.pdf`. Which format/page count/binding to use is read straight off the document's own `<html data-page-format data-page-count dir>` (`HtmlRenderer` now stamps these; `src/rendering/print_metadata.py` reads them back), so the edited copy prints identically to what the generator itself would have produced — steadier than relying on whatever the browser's print dialog happens to default to. Falls back to `window.print()` only if `/print` is unreachable (e.g. `workbook.html` opened as a local file with no server behind it). New tests in `tests/test_print_metadata.py`, `tests/test_rendering.py`, `tests/test_web.py`.
