__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Local secrets (OPENROUTER_API_KEY, ...) — never committed.
.env

# Generated workbooks. One sample run is committed so the artifacts are
# reviewable without running anything.
output/*
!output/kfar-hanokdim/
