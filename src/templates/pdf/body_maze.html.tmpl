    <div class="maze-page">
      <div class="maze-endcap-row">
$start_endcap
      </div>
      <div class="maze-stage">
        <svg class="maze-grid" viewBox="0 0 $columns $rows" preserveAspectRatio="xMidYMid meet" aria-hidden="true" focusable="false">
          <path class="maze-walls" d="$walls" />
        </svg>
      </div>
      <div class="maze-endcap-row">
$goal_endcap
      </div>
    </div>
